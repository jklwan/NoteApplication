<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chends.note">

    <application
        android:name=".NoteApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name="com.chends.note.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.MainActivity"
            android:label="Main"
            android:screenOrientation="portrait" />
        <activity
            android:name=".recyclerview.RecyclerDecorationActivity"
            android:label="RecyclerView"
            android:screenOrientation="portrait" />
        <activity
            android:name=".business.ToastActivity"
            android:label="Toast"
            android:screenOrientation="portrait" />
        <activity
            android:name=".business.LanguageActivity"
            android:label="Language"
            android:screenOrientation="portrait" />
        <activity
            android:name=".recyclerview.RecyclerSideActivity"
            android:label="RecyclerSideActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".recyclerview.RecyclerSideInViewPagerActivity"
            android:label="RecyclerSideInViewPagerActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".business.ViewBindingActivity"
            android:label="ViewBinding"
            android:screenOrientation="portrait" />
    </application>

</manifest>